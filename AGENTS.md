# AGENTS.md — PETIT 開発ルール

AIコーディングエージェントと開発者向けの共通ルールです。  
製品の目的・仕様は `Concept.md`、`README.md`、`docs/` を参照してください。

---

## PETIT概要

PETITは、会話から意図を理解し、生活・制作・開発・タスク・予定・記憶を支援する個人用AIアシスタントです。

- Backend: FastAPI + Python
- LLM: LM StudioなどのOpenAI互換API
- 記憶: SQLite。必要に応じてChroma・Obsidian Markdownを利用
- Frontend: HTML / CSS / Vanilla JavaScript
- MVP: localhostで動くテキストチャット

主な構成:

```text
backend/    API・エージェント・ツール・データ処理
frontend/   ブラウザUI
scripts/    セットアップ・運用スクリプト
storage/    実行時データ（Git管理外）
docs/       仕様・検証手順
```

---

## 基本方針

- 日本語で簡潔に説明する
- 推測を事実として扱わない
- 最小限の変更で目的を達成する
- 既存の設計・命名・API・データ形式を尊重する
- 無関係な変更を混ぜない
- 未確認事項を完了扱いしない
- 変更後は結果・未確認事項・次の作業を報告する

---

## 作業開始

可能な範囲で次を確認する。

1. `AGENTS.md`
2. `Concept.md`
3. `README.md`
4. `PROGRESS.md`
5. Git状態
6. 関連Issue・PR

```bash
git status
git branch --show-current
```

未コミット変更は勝手に変更・破棄せず、今回の変更と混ぜない。

---

## 調査

- エラー、関数名、設定名から対象を絞る
- 必要なファイルだけを読む
- 最近の変更、Git履歴、Issue、PRも確認する
- 大きなログやファイル全文をそのまま出力しない
- 外部仕様は公式ドキュメントを優先する

---

## Issue

次は原則Issueを作成してから対応する。

- 不具合
- 機能追加
- 設計変更
- 複数ファイルの修正
- 継続作業

軽微な文言修正、コメント修正、整形のみなら不要。

---

## 実装

- Issueの目的と完了条件に沿って実装する
- 依存追加前に標準ライブラリで解決できないか確認する
- LM Studioや外部サービス停止時も、可能な範囲でサーバーを継続し原因を表示する
- ツール追加時は登録・引数検証・権限・確認フローまで確認する
- 書き込み操作は既存の承認・冪等性・監査境界を壊さない
- 秘密情報をコード、ログ、Issue、PRへ含めない
- 環境依存値は設定へ分離する
- 返答は必要に応じて「今やる1個 / 次 / 後で」に整理する

---

## テスト

変更に応じて可能な範囲で実施する。

- 構文確認
- 型チェック
- 単体・回帰テスト
- Build
- 実ブラウザ・実サービスE2E

未実施の確認は、理由と人間が確認すべき内容を明記する。CI失敗中は完了扱いにしない。

---

## Git・Pull Request

- `main`をPRのbaseとし、直接コミットしない
- 作業ごとに `feat/` `fix/` `docs/` `refactor/` などの専用ブランチを使う
- 意味のある単位でコミットし、接頭辞付きの簡潔な日本語メッセージを書く
- リモート操作はユーザーの依頼範囲内で行う

PRには最低限、次を記載する。

- 変更概要
- 変更理由
- 影響範囲
- テスト結果
- 未確認事項
- 関連Issue

完全対応のみ `Closes #123`、一部対応は `Refs #123` を使う。

---

## Progress Log

進捗は `PROGRESS.md` に記録する。

- 作業開始時に「現在の状態 / 未確認・TODO」と直近履歴を読む
- 変更・調査・設計判断・テストを行ったら履歴へ1行追記する
- 履歴は追記専用。既存行を削除・要約・上書きしない
- 未確認事項や次の作業が変わった場合だけ、現在状態を更新する
- 実施していないテストを成功と書かない

```md
| YYYY-MM-DD | HH:mm | #N | 変更内容（1行サマリー） |
```

時刻はUTCを使用する。

---

## ドキュメント

仕様、API、設定、データ形式、運用方法が変わった場合は関連文書も更新する。

---

## 禁止事項

- 未確認を完了扱いする
- テスト未実施で成功と書く
- Issue作成だけで終了する
- CI失敗中に完了扱いする
- 無関係な変更を混ぜる
- 未コミット変更を勝手に扱う
- 秘密情報を含める
- 既存データを無言で削除・全面置換する

---

## 完了報告

簡潔に次を報告する。

- 変更内容
- テスト結果
- 関連Issue・PR
- 未確認事項
- 次の作業
