# AGENTS.md — PETIT 開発ルール

AIコーディングエージェントと開発者が、PETITを継続的に開発するための共通ルールです。
製品の目的・思想は `Concept.md`、利用方法は `README.md`、詳細仕様は `docs/` を参照してください。

---

## 1. 基本方針

- 日本語で簡潔に説明する。
- 推測を事実として扱わず、未確認の内容を完了扱いにしない。
- 作業前に、原因・方針・影響範囲を短く整理する。
- 最小限の変更で目的を達成する。
- 既存の設計・API・命名・データ形式・運用を尊重する。
- 無関係な変更、削除、移動、統合、リネームを行わない。
- 大きな構成変更や破壊的変更は、必要性と移行方法を明記する。
- 変更後は、結果・確認内容・未確認事項・次の作業を報告する。

---

## 2. プロジェクト概要

PETITは「会話から色々やってくれる自分専用AIアシスタント」です。

- ローカルLLM（LM StudioなどのOpenAI互換エンドポイント）を利用する。
- 現在はFastAPI + ブラウザのテキストチャットを中心に開発する。
- 最終形は、スマホとPCから使える音声中心の常駐アシスタントを目指す。
- 単語トリガー型ではなく、意図理解 → ツール選択 → 実行 → 自然文返答で処理する。
- 予定・タスク・記憶は、必要に応じて「今やる1個 / 次 / 後で」に整理する。

主な構成：

- Backend: FastAPI + Python
- LLM: LM Studio互換 `/v1/chat/completions`
- 記憶・状態: SQLite、Markdown / Obsidian、必要に応じてEmbedding検索
- Frontend: HTML / CSS / JavaScript
- 外部連携: Notion、Calendar、Linkraft、GitHubなど

主要ディレクトリ：

```text
backend/   API・エージェント・ツール・永続化
frontend/  チャットUI・音声UI
docs/      設計・連携・検証手順
tests/     回帰テスト
tools/     単体ツール
scripts/   セットアップ・運用スクリプト
storage/   実行時データ（原則git管理外）
```

---

## 3. 作業開始

可能な範囲で、次を確認してから変更する。

1. `AGENTS.md`
2. `Concept.md`
3. `README.md`
4. `PROGRESS.md`
5. 関連する `docs/`
6. Gitの現在ブランチと作業ツリー
7. 関連Issue・Pull Request・直近の変更

ローカル作業では最低限、次を確認する。

```bash
git status
git branch --show-current
```

- 未コミット変更は、内容を確認せず上書き・破棄・混在させない。
- 作業再開時は、コード変更より先に `PROGRESS.md` の未確認事項と直近履歴を読む。
- 文書と実装が食い違う場合は、実装・テスト・現在の運用を確認し、必要なら文書も修正する。

---

## 4. 調査

- 最初から全体を読むのではなく、検索で対象を絞る。
- エラー文、関数名、クラス名、API名、設定名を手掛かりに追う。
- 症状と原因を分け、再現条件と失敗箇所を確認する。
- 関連テスト、Git履歴、最近のPR、Issueも確認する。
- ログ全文や巨大な検索結果をそのまま出力せず、必要な箇所を要約する。
- 外部ライブラリやAPIの仕様は公式ドキュメントを優先する。
- 原因が未確定なら「調査中」と明記する。

---

## 5. Issue・ブランチ・コミット

### Issue

次は原則、既存Issueを検索し、なければ実装前に作成する。

- 不具合
- 機能追加
- 仕様・設計変更
- 複数ファイルにまたがる修正
- 継続作業や完了条件を管理すべき作業

タイポ、コメント、文言、挙動を変えない整形などの軽微な変更は省略してよい。
Issueには必要に応じて、背景・目的・現状・原因・方針・影響範囲・完了条件・非目標を書く。

### ブランチ

- `main` を安定版かつPRのbaseとし、直接コミットしない。
- 作業ごとに専用ブランチを作る。
- 1ブランチでは原則1つの目的だけを扱う。
- 無関係な変更や既存の未コミット変更を混ぜない。

例：

```text
agent/fix-schedule-date-parsing
feat/task-area-model
fix/empty-model-response
docs/update-agents-guidelines
```

### コミット

- 意味のある単位で分ける。
- 日本語で簡潔に書く。
- `feat:` `fix:` `docs:` `refactor:` `perf:` `test:` `chore:` `ci:` などを使用する。
- 秘密情報や利用者の無関係な変更を含めない。
- Pushは、ユーザーがGitHub上の変更・Push・PR作成を依頼した場合に行う。

---

## 6. 実装と安全性

- Issueの目的と完了条件に沿って実装する。
- 既存API、設定、DB、イベント、キャッシュ、Tool Schemaを不用意に壊さない。
- 例外処理やフォールバックで問題を隠さない。
- エラーを握りつぶさず、利用者が理解できる状態で返す。
- 秘密情報、APIキー、トークン、パスワード、個人情報をコード・ログ・Issue・PRへ含めない。
- 環境依存値は環境変数や設定へ分離する。
- 書き込み、削除、送信、公開などの副作用がある操作は、既存の確認・Approval・Idempotency・Audit境界を維持する。
- 未確認候補、類似名、別プロジェクトの情報を自動統合しない。
- staleな外部データは、最新情報として断定せず古さを明示する。
- BRAIN操作では、Vault外・Markdown以外・`_private/`を許可なく扱わない。

ツールを追加・変更する場合は、最低限次を確認する。

- 実装モジュール内でTool Schemaとハンドラーを登録する。
- `backend/tools/__init__.py` から対象モジュールをimportし、登録漏れを防ぐ。
- `registered_names`、OpenAI Tool Schema、dispatch、確認要否の整合性をテストする。
- 読み取りと書き込みの安全境界を混同しない。

---

## 7. PETIT固有の実装方針

- 依存は最小限とし、まず標準ライブラリや既存依存で解けないか考える。
- LM Studioが停止・タイムアウト・空応答でも、PETIT全体を落とさず分かるエラーまたは安全なフォールバックを返す。
- Chat、Agent、Toolの役割を分け、軽い会話で不要な重い処理を行わない。
- LLMには意図判断とTool Callを任せ、引数検証・権限・副作用の実行はPython側で制御する。
- 保存済み事実、同期結果、推論、未確認候補を区別する。
- commit、CI成功、PR merge、実画面確認、デプロイ、本番確認を同じ「完了」として扱わない。

---

## 8. テストと確認

変更内容に応じて、可能な範囲で次を実行する。

- 構文確認・`compileall`
- 静的解析・型チェック
- 関連Unit Test
- 回帰テスト
- Build
- API・ブラウザ・実機確認
- LM Studio、Notion、Calendarなど実サービスを使うE2E
- GitHub Actions / CI

テストできない場合は、次を明記する。

- 実施できなかった確認
- 理由
- 代わりに確認した内容
- 人間が確認すべき手順

ドキュメントのみの変更では、差分、リンク、記載と実運用の整合性を確認し、実行テストが不要であることを明記する。
テスト未実施のまま「問題なし」「完了」と断定しない。

---

## 9. Pull Request

実装後は原則Pull Requestを作成する。
PRには最低限、次を記載する。

- 変更概要
- 背景・変更理由
- 主な変更点
- 影響範囲と安全性
- テスト結果
- 未確認事項
- 実画面・実機で確認すべきこと
- 関連Issue

Issueを完全に解決する場合：

```text
Closes #123
```

一部対応の場合：

```text
Refs #123
```

- 完了条件を満たしていない場合は `Closes` を使わない。
- CI失敗中、レビュー待ち、実画面未確認を完了扱いにしない。
- 未完成・段階実装・検証中はDraft PRを利用する。
- PR本文と実際の差分を一致させる。

---

## 10. Progress Logとドキュメント

このプロジェクトでは進捗を `PROGRESS.md` に記録する。

### 作業開始時

- 「現在の状態 / 未確認・TODO」を読む。
- 直近の履歴を確認する。

### 作業終了時

- 変更・調査・設計判断・テストを行った場合は、履歴へ1行追記する。
- 履歴は追記専用で、既存行を削除・要約・上書きしない。
- 未確認事項や次の作業が変わった場合だけ、現在状態を更新する。

```md
| YYYY-MM-DD | HH:mm | #N | 変更内容（1行サマリー） |
```

- 時刻はUTCで記録する。
- 実際に行ったことだけを書く。
- 未確認は「未確認」、未実施は「未実施」と書く。

次が変わった場合は、README・`docs/`・設定例も更新する。

- セットアップ方法
- 環境変数
- API・Tool Schema・データ構造
- 利用方法
- 重要な設計判断
- 制限事項・既知の問題
- 検証・運用手順

---

## 11. 完了判定

作業は、必要な範囲で次を満たしたときに完了とする。

- 完了条件を満たしている。
- 必要な実装・文書が対象ブランチに存在する。
- 関連テストとCIが成功している。
- 未確認事項が明記されている。
- 必要な実画面・実機・本番確認が記録されている。
- 後続作業がIssueまたは残作業として記録されている。

次の状態を区別する。

- 実装済み
- 自動テスト確認済み
- 実画面確認済み
- デプロイ済み
- 本番確認済み
- 完了
- 中断
- 保留

---

## 12. 禁止事項

- 未確認を完了扱いにする。
- テスト未実施なのに成功と書く。
- Issueを作成しただけで終了する。
- CI失敗中のPRを根拠なくマージする。
- mainへ直接コミットする。
- 無関係な変更を混ぜる。
- 既存の変更を勝手に破棄する。
- 秘密情報をコード・ログ・Issue・PRへ載せる。
- エラーを握りつぶす。
- 副作用のある処理を確認なしで実行する。
- 未確認候補を正本へ自動統合する。
- 実装とドキュメントを矛盾させる。

---

## 13. 完了報告

簡潔に次を報告する。

- 変更内容と変更ファイル
- 実行したテスト・CI結果
- 関連Issue・PR
- 未確認事項
- 次の具体的な作業
